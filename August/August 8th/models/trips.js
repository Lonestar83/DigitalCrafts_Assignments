class Trip {
    constructor(tripTitle, imageURL, departureDate, returnDate) {
        this.tripTitle = tripTitle
        this.imageURL = imageURL
        this.departureDate = departureDate
        this.returnDate = returnDate
    }
}

module.exports = Trip